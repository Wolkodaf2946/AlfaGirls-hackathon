Build app:
```bash
docker compose build
```

Run app:
```bash
docker compose up -d
```

.env:
```
CONFIG_PATH=./configs/server/local.yaml
DB_HOST= localhost | db
DB_PASSWORD=
DB_USERNAME=
DB_NAME=
SIGNING_KEY=
SALT=
```